---
title: Tag | Thẻ
description: Tag component for labels and categorization | Component thẻ cho nhãn và phân loại
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

The **Tag** component is used for displaying labels, categories, status indicators, and other metadata. Inspired by Ant Design's Tag component with support for closable tags, status colors, and custom styling.

**Tag** là component được sử dụng để hiển thị nhãn, danh mục, chỉ báo trạng thái và metadata khác. Lấy cảm hứng từ component Tag của Ant Design với hỗ trợ cho thẻ có thể đóng, màu trạng thái và kiểu tùy chỉnh.

## Features | Tính Năng

- **Status colors** | **Màu trạng thái**: 6 predefined status colors (basic, primary, success, info, warning, danger)
- **Closable tags** | **Thẻ có thể đóng**: Removable tags with close button
- **Custom colors** | **Màu tùy chỉnh**: Support for custom color values
- **Icon support** | **Hỗ trợ icon**: Add icons to tags
- **Multiple sizes** | **Nhiều kích thước**: Small, medium, and large sizes
- **Checkable tags** | **Thẻ có thể đánh dấu**: Toggle selection state
- **Dark mode** | **Chế độ tối**: Full dark mode support
- **Rounded variants** | **Biến thể bo tròn**: Different border radius options

## Installation | Cài Đặt

<Tabs>
  <TabItem label="CLI">
    ```bash
    npx galaxy-ui-cli@latest add tag
    ```
  </TabItem>
  <TabItem label="Manual | Thủ Công">
    Copy the component code from the [source](https://github.com/buikevin/galaxy-ui-cli/tree/main/packages/angular/src/lib/tag).
  </TabItem>
</Tabs>

## Usage | Sử Dụng

### Basic Usage | Sử Dụng Cơ Bản

```typescript
import { Component } from '@angular/core';
import { TagComponent } from '@/components/ui/tag';

@Component({
  selector: 'app-example',
  standalone: true,
  imports: [TagComponent],
  template: `
    <div class="flex gap-2">
      <g-tag>Default | Mặc định</g-tag>
      <g-tag status="primary">Primary | Chính</g-tag>
      <g-tag status="success">Success | Thành công</g-tag>
      <g-tag status="info">Info | Thông tin</g-tag>
      <g-tag status="warning">Warning | Cảnh báo</g-tag>
      <g-tag status="danger">Danger | Nguy hiểm</g-tag>
    </div>
  `
})
export class ExampleComponent {}
```

### Closable Tags | Thẻ Có Thể Đóng

```typescript
import { signal } from '@angular/core';

@Component({
  template: `
    <div class="flex flex-wrap gap-2">
      @for (tag of tags(); track tag.id) {
        <g-tag
          [status]="tag.status"
          [closable]="true"
          (close)="removeTag(tag.id)">
          {{ tag.label }}
        </g-tag>
      }
    </div>

    @if (tags().length === 0) {
      <p class="text-gray-500">No tags | Không có thẻ</p>
    }
  `
})
export class ClosableTagsComponent {
  tags = signal([
    { id: 1, label: 'Angular', status: 'primary' },
    { id: 2, label: 'TypeScript', status: 'info' },
    { id: 3, label: 'RxJS', status: 'success' },
    { id: 4, label: 'Signals', status: 'warning' }
  ]);

  removeTag(id: number) {
    this.tags.update(tags => tags.filter(tag => tag.id !== id));
  }
}
```

### With Icons | Với Icon

```typescript
@Component({
  template: `
    <div class="flex flex-wrap gap-2">
      <g-tag status="primary">
        <span class="flex items-center gap-1">
          👤 User | Người dùng
        </span>
      </g-tag>

      <g-tag status="success">
        <span class="flex items-center gap-1">
          ✓ Active | Hoạt động
        </span>
      </g-tag>

      <g-tag status="warning">
        <span class="flex items-center gap-1">
          ⚠️ Pending | Chờ xử lý
        </span>
      </g-tag>

      <g-tag status="danger">
        <span class="flex items-center gap-1">
          ✗ Blocked | Chặn
        </span>
      </g-tag>

      <g-tag status="info">
        <span class="flex items-center gap-1">
          🔒 Private | Riêng tư
        </span>
      </g-tag>
    </div>
  `
})
export class IconTagsComponent {}
```

### Different Sizes | Các Kích Thước Khác Nhau

```typescript
@Component({
  template: `
    <div class="space-y-4">
      <div class="flex items-center gap-2">
        <g-tag size="sm" status="primary">Small | Nhỏ</g-tag>
        <g-tag size="md" status="primary">Medium | Vừa</g-tag>
        <g-tag size="lg" status="primary">Large | Lớn</g-tag>
      </div>

      <div class="flex items-center gap-2">
        <g-tag size="sm" status="success" [closable]="true">Small Closable</g-tag>
        <g-tag size="md" status="success" [closable]="true">Medium Closable</g-tag>
        <g-tag size="lg" status="success" [closable]="true">Large Closable</g-tag>
      </div>
    </div>
  `
})
export class SizedTagsComponent {}
```

### Checkable Tags | Thẻ Có Thể Đánh Dấu

```typescript
@Component({
  template: `
    <div class="space-y-4">
      <p class="font-semibold">Select categories | Chọn danh mục:</p>

      <div class="flex flex-wrap gap-2">
        @for (category of categories; track category.id) {
          <g-tag
            [checkable]="true"
            [checked]="isSelected(category.id)"
            (checkedChange)="toggleCategory(category.id)">
            {{ category.label }}
          </g-tag>
        }
      </div>

      <div class="p-4 bg-gray-100 dark:bg-gray-800 rounded">
        <p class="font-semibold mb-2">Selected | Đã chọn:</p>
        <p>{{ getSelectedLabels() || 'None | Không có' }}</p>
      </div>
    </div>
  `
})
export class CheckableTagsComponent {
  selectedIds = signal(new Set<number>());

  categories = [
    { id: 1, label: 'Technology | Công nghệ' },
    { id: 2, label: 'Design | Thiết kế' },
    { id: 3, label: 'Business | Kinh doanh' },
    { id: 4, label: 'Marketing' },
    { id: 5, label: 'Science | Khoa học' },
    { id: 6, label: 'Health | Sức khỏe' }
  ];

  isSelected(id: number): boolean {
    return this.selectedIds().has(id);
  }

  toggleCategory(id: number) {
    this.selectedIds.update(ids => {
      const newIds = new Set(ids);
      if (newIds.has(id)) {
        newIds.delete(id);
      } else {
        newIds.add(id);
      }
      return newIds;
    });
  }

  getSelectedLabels(): string {
    const selected = this.categories
      .filter(cat => this.selectedIds().has(cat.id))
      .map(cat => cat.label);
    return selected.join(', ');
  }
}
```

### Custom Colors | Màu Tùy Chỉnh

```typescript
@Component({
  template: `
    <div class="flex flex-wrap gap-2">
      <g-tag [color]="'#f50'">Red | Đỏ</g-tag>
      <g-tag [color]="'#2db7f5'">Cyan | Xanh lơ</g-tag>
      <g-tag [color]="'#87d068'">Green | Xanh lá</g-tag>
      <g-tag [color]="'#108ee9'">Blue | Xanh dương</g-tag>
      <g-tag [color]="'#f5317f'">Magenta</g-tag>
      <g-tag [color]="'#722ed1'">Purple | Tím</g-tag>
      <g-tag [color]="'#fa8c16'">Orange | Cam</g-tag>
      <g-tag [color]="'#eb2f96'">Pink | Hồng</g-tag>
    </div>
  `
})
export class CustomColorTagsComponent {}
```

### Rounded Variants | Biến Thể Bo Tròn

```typescript
@Component({
  template: `
    <div class="space-y-4">
      <div class="flex gap-2">
        <g-tag status="primary" rounded="none">Square | Vuông</g-tag>
        <g-tag status="primary" rounded="sm">Small Rounded | Bo nhỏ</g-tag>
        <g-tag status="primary" rounded="md">Medium Rounded | Bo vừa</g-tag>
        <g-tag status="primary" rounded="lg">Large Rounded | Bo lớn</g-tag>
        <g-tag status="primary" rounded="full">Pill | Viên</g-tag>
      </div>
    </div>
  `
})
export class RoundedTagsComponent {}
```

### Dynamic Tag Input | Nhập Thẻ Động

```typescript
@Component({
  template: `
    <div class="space-y-4">
      <div class="flex flex-wrap gap-2">
        @for (tag of tags(); track tag) {
          <g-tag
            status="primary"
            [closable]="true"
            (close)="removeTag(tag)">
            {{ tag }}
          </g-tag>
        }

        @if (inputVisible()) {
          <input
            #tagInput
            type="text"
            [(ngModel)]="inputValue"
            (blur)="handleInputConfirm()"
            (keyup.enter)="handleInputConfirm()"
            class="w-24 px-2 py-1 border rounded text-sm"
            placeholder="New tag">
        } @else {
          <button
            (click)="showInput()"
            class="px-2 py-1 border border-dashed rounded text-sm hover:border-blue-500 hover:text-blue-500">
            + New Tag | Thẻ mới
          </button>
        }
      </div>
    </div>
  `
})
export class DynamicTagInputComponent {
  tags = signal(['Tag 1', 'Tag 2', 'Tag 3']);
  inputVisible = signal(false);
  inputValue = '';

  @ViewChild('tagInput') tagInput!: ElementRef<HTMLInputElement>;

  showInput() {
    this.inputVisible.set(true);
    setTimeout(() => this.tagInput?.nativeElement.focus(), 0);
  }

  handleInputConfirm() {
    const value = this.inputValue.trim();
    if (value && !this.tags().includes(value)) {
      this.tags.update(tags => [...tags, value]);
    }
    this.inputValue = '';
    this.inputVisible.set(false);
  }

  removeTag(tag: string) {
    this.tags.update(tags => tags.filter(t => t !== tag));
  }
}
```

## Props | Thuộc Tính

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `status` | `'basic' \| 'primary' \| 'success' \| 'info' \| 'warning' \| 'danger'` | `'basic'` | Status color theme <br/> Màu chủ đề trạng thái |
| `color` | `string` | - | Custom color (hex/rgb) <br/> Màu tùy chỉnh (hex/rgb) |
| `closable` | `boolean` | `false` | Show close button <br/> Hiển thị nút đóng |
| `checkable` | `boolean` | `false` | Enable toggle selection <br/> Bật chọn chuyển đổi |
| `checked` | `boolean` | `false` | Checked state <br/> Trạng thái đã đánh dấu |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Tag size <br/> Kích thước thẻ |
| `rounded` | `'none' \| 'sm' \| 'md' \| 'lg' \| 'full'` | `'md'` | Border radius <br/> Bán kính viền |
| `bordered` | `boolean` | `true` | Show border <br/> Hiển thị viền |
| `icon` | `TemplateRef` | - | Custom icon template <br/> Template icon tùy chỉnh |

### Outputs | Sự Kiện

| Event | Type | Description |
|-------|------|-------------|
| `close` | `void` | Emitted when close button is clicked <br/> Phát ra khi nút đóng được nhấp |
| `checkedChange` | `boolean` | Emitted when checked state changes <br/> Phát ra khi trạng thái đánh dấu thay đổi |
| `click` | `MouseEvent` | Emitted when tag is clicked <br/> Phát ra khi thẻ được nhấp |

## Accessibility | Khả Năng Tiếp Cận

- Semantic HTML with proper roles
- Keyboard navigation support (Tab, Enter, Space)
- ARIA labels for close button
- Focus visible indicators
- Screen reader friendly
- Accessible color contrast

## Examples | Ví Dụ

### Category Tags | Thẻ Danh Mục

```typescript
@Component({
  template: `
    <div class="space-y-6">
      <article class="border rounded-lg p-6">
        <h2 class="text-2xl font-bold mb-2">
          Building Modern Web Applications with Angular
        </h2>

        <div class="flex gap-2 mb-4">
          <g-tag status="primary">Angular</g-tag>
          <g-tag status="info">TypeScript</g-tag>
          <g-tag status="success">Web Development | Phát triển Web</g-tag>
          <g-tag>Tutorial</g-tag>
        </div>

        <p class="text-gray-600 dark:text-gray-400 mb-4">
          Learn how to build modern, scalable web applications using Angular...
        </p>

        <div class="flex justify-between items-center text-sm text-gray-500">
          <span>By John Doe</span>
          <span>October 29, 2024</span>
        </div>
      </article>

      <article class="border rounded-lg p-6">
        <h2 class="text-2xl font-bold mb-2">
          Mastering CSS Grid and Flexbox
        </h2>

        <div class="flex gap-2 mb-4">
          <g-tag status="warning">CSS</g-tag>
          <g-tag status="info">Design | Thiết kế</g-tag>
          <g-tag>Beginner | Người mới</g-tag>
        </div>

        <p class="text-gray-600 dark:text-gray-400 mb-4">
          A comprehensive guide to modern CSS layout techniques...
        </p>

        <div class="flex justify-between items-center text-sm text-gray-500">
          <span>By Jane Smith</span>
          <span>October 28, 2024</span>
        </div>
      </article>
    </div>
  `
})
export class CategoryTagsComponent {}
```

### Filter Chips | Chip Lọc

```typescript
@Component({
  template: `
    <div class="space-y-4">
      <div class="flex justify-between items-center">
        <h3 class="text-lg font-semibold">Products | Sản phẩm ({{ filteredCount }})</h3>
        @if (activeFilters().size > 0) {
          <button (click)="clearAllFilters()" class="text-sm text-blue-600 hover:underline">
            Clear all | Xóa tất cả
          </button>
        }
      </div>

      <div class="space-y-2">
        <p class="text-sm font-medium">Category | Danh mục:</p>
        <div class="flex flex-wrap gap-2">
          @for (category of categories; track category) {
            <g-tag
              [checkable]="true"
              [checked]="activeFilters().has('category:' + category)"
              (checkedChange)="toggleFilter('category', category)">
              {{ category }}
            </g-tag>
          }
        </div>
      </div>

      <div class="space-y-2">
        <p class="text-sm font-medium">Price Range | Khoảng giá:</p>
        <div class="flex flex-wrap gap-2">
          @for (range of priceRanges; track range.label) {
            <g-tag
              [checkable]="true"
              [checked]="activeFilters().has('price:' + range.label)"
              (checkedChange)="toggleFilter('price', range.label)">
              {{ range.label }}
            </g-tag>
          }
        </div>
      </div>

      @if (activeFilters().size > 0) {
        <div class="p-4 bg-blue-50 dark:bg-blue-900/20 rounded">
          <p class="text-sm font-medium mb-2">Active filters | Bộ lọc đang hoạt động:</p>
          <div class="flex flex-wrap gap-2">
            @for (filter of Array.from(activeFilters()); track filter) {
              <g-tag
                status="primary"
                [closable]="true"
                (close)="removeFilter(filter)">
                {{ filter.split(':')[1] }}
              </g-tag>
            }
          </div>
        </div>
      }
    </div>
  `
})
export class FilterChipsComponent {
  Array = Array;
  activeFilters = signal(new Set<string>());
  filteredCount = 156;

  categories = ['Electronics | Điện tử', 'Clothing | Quần áo', 'Books | Sách', 'Food | Thực phẩm'];
  priceRanges = [
    { label: 'Under $50 | Dưới $50', value: [0, 50] },
    { label: '$50-$100', value: [50, 100] },
    { label: '$100-$200', value: [100, 200] },
    { label: 'Over $200 | Trên $200', value: [200, Infinity] }
  ];

  toggleFilter(type: string, value: string) {
    const filterKey = `${type}:${value}`;
    this.activeFilters.update(filters => {
      const newFilters = new Set(filters);
      if (newFilters.has(filterKey)) {
        newFilters.delete(filterKey);
      } else {
        newFilters.add(filterKey);
      }
      return newFilters;
    });
  }

  removeFilter(filterKey: string) {
    this.activeFilters.update(filters => {
      const newFilters = new Set(filters);
      newFilters.delete(filterKey);
      return newFilters;
    });
  }

  clearAllFilters() {
    this.activeFilters.set(new Set());
  }
}
```

### Skill Tags | Thẻ Kỹ Năng

```typescript
@Component({
  template: `
    <div class="max-w-2xl mx-auto space-y-6">
      <div class="border rounded-lg p-6">
        <div class="flex items-start gap-4">
          <img src="/avatars/john.jpg" alt="John Doe" class="w-20 h-20 rounded-full">
          <div class="flex-1">
            <h3 class="text-xl font-bold mb-1">John Doe</h3>
            <p class="text-gray-600 dark:text-gray-400 mb-3">Senior Full Stack Developer</p>

            <div class="space-y-3">
              <div>
                <p class="text-sm font-medium mb-2">Primary Skills | Kỹ năng chính:</p>
                <div class="flex flex-wrap gap-2">
                  <g-tag status="primary" size="sm">Angular</g-tag>
                  <g-tag status="primary" size="sm">TypeScript</g-tag>
                  <g-tag status="primary" size="sm">Node.js</g-tag>
                  <g-tag status="primary" size="sm">MongoDB</g-tag>
                </div>
              </div>

              <div>
                <p class="text-sm font-medium mb-2">Secondary Skills | Kỹ năng phụ:</p>
                <div class="flex flex-wrap gap-2">
                  <g-tag status="info" size="sm">React</g-tag>
                  <g-tag status="info" size="sm">Vue.js</g-tag>
                  <g-tag status="info" size="sm">Docker</g-tag>
                  <g-tag status="info" size="sm">AWS</g-tag>
                </div>
              </div>

              <div>
                <p class="text-sm font-medium mb-2">Learning | Đang học:</p>
                <div class="flex flex-wrap gap-2">
                  <g-tag status="warning" size="sm">Rust</g-tag>
                  <g-tag status="warning" size="sm">GraphQL</g-tag>
                  <g-tag status="warning" size="sm">Kubernetes</g-tag>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  `
})
export class SkillTagsComponent {}
```

## Best Practices | Thực Hành Tốt Nhất

1. **Use meaningful colors** | **Dùng màu có ý nghĩa**: Match colors to tag purpose (success for active, danger for errors)

2. **Limit tag count** | **Giới hạn số lượng thẻ**: Don't overwhelm with too many tags

3. **Consistent sizing** | **Kích thước nhất quán**: Use consistent sizes within the same context

4. **Clear labels** | **Nhãn rõ ràng**: Keep tag text short and descriptive

5. **Interactive feedback** | **Phản hồi tương tác**: Provide visual feedback for closable and checkable tags

6. **Accessible contrast** | **Độ tương phản dễ tiếp cận**: Ensure sufficient color contrast

## Related Components | Components Liên Quan

- [Badge](../../other/badge) - Notification badges | Huy hiệu thông báo
- [List](./list) - List with tags | Danh sách với thẻ
- [Table](./table) - Table with status tags | Bảng với thẻ trạng thái
