---
title: Autocomplete | Tự Động Hoàn Thành
description: Autocomplete input with suggestions | Ô nhập tự động hoàn thành với gợi ý
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

The **Autocomplete** component provides a search input with dropdown suggestions for enhanced user experience.

**Autocomplete** là component cung cấp ô nhập tìm kiếm với gợi ý thả xuống để nâng cao trải nghiệm người dùng.

## Features | Tính Năng

- **Search filtering** | **Lọc tìm kiếm**: Filter options as user types
- **Custom templates** | **Mẫu tùy chỉnh**: Customize option display
- **Loading state** | **Trạng thái đang tải**: Show loading indicator during async operations
- **Empty state** | **Trạng thái rỗng**: Custom message when no results
- **Angular Forms integration** | **Tích hợp Angular Forms**: Works with ngModel and Reactive Forms
- **Keyboard navigation** | **Điều hướng bàn phím**: Arrow keys and Enter to select
- **Dark mode** | **Chế độ tối**: Full dark mode support

## Installation | Cài Đặt

<Tabs>
  <TabItem label="CLI">
    ```bash
    npx galaxy-ui-cli@latest add autocomplete
    ```
  </TabItem>
  <TabItem label="Manual | Thủ Công">
    Copy the component code from the [source](https://github.com/buikevin/galaxy-ui-cli/tree/main/packages/angular/src/lib/autocomplete).
  </TabItem>
</Tabs>

## Usage | Sử Dụng

### Basic Usage | Sử Dụng Cơ Bản

```typescript
import { Component, signal } from '@angular/core';
import { AutocompleteComponent } from '@/components/ui/autocomplete';

@Component({
  selector: 'app-example',
  standalone: true,
  imports: [AutocompleteComponent],
  template: `
    <g-autocomplete
      [options]="options"
      [(value)]="selectedValue"
      placeholder="Search... | Tìm kiếm...">
    </g-autocomplete>

    <p>Selected: {{ selectedValue() }}</p>
  `
})
export class ExampleComponent {
  selectedValue = signal('');

  options = [
    { label: 'Apple', value: 'apple' },
    { label: 'Banana', value: 'banana' },
    { label: 'Cherry', value: 'cherry' },
    { label: 'Date', value: 'date' },
    { label: 'Elderberry', value: 'elderberry' }
  ];
}
```

### With Async Data | Với Dữ Liệu Bất Đồng Bộ

```typescript
@Component({
  template: `
    <g-autocomplete
      [options]="filteredOptions()"
      [(value)]="selectedValue"
      [loading]="isLoading()"
      (search)="onSearch($event)"
      placeholder="Search users... | Tìm người dùng...">
    </g-autocomplete>
  `
})
export class AsyncAutocompleteComponent {
  selectedValue = signal('');
  filteredOptions = signal<AutocompleteOption[]>([]);
  isLoading = signal(false);

  async onSearch(query: string) {
    if (!query) {
      this.filteredOptions.set([]);
      return;
    }

    this.isLoading.set(true);

    try {
      // Simulate API call | Mô phỏng gọi API
      const results = await this.searchUsers(query);
      this.filteredOptions.set(results);
    } finally {
      this.isLoading.set(false);
    }
  }

  async searchUsers(query: string): Promise<AutocompleteOption[]> {
    return new Promise(resolve => {
      setTimeout(() => {
        const users = [
          { label: 'John Doe', value: 'john' },
          { label: 'Jane Smith', value: 'jane' },
          { label: 'Bob Johnson', value: 'bob' }
        ];
        resolve(users.filter(u =>
          u.label.toLowerCase().includes(query.toLowerCase())
        ));
      }, 500);
    });
  }
}
```

### With Custom Template | Với Mẫu Tùy Chỉnh

```typescript
@Component({
  template: `
    <g-autocomplete
      [options]="countries"
      [(value)]="selectedCountry"
      placeholder="Select country... | Chọn quốc gia...">
      <ng-template #optionTemplate let-option>
        <div class="flex items-center gap-2">
          <span class="text-xl">{{ option.flag }}</span>
          <div>
            <div class="font-medium">{{ option.label }}</div>
            <div class="text-xs text-gray-500">{{ option.code }}</div>
          </div>
        </div>
      </ng-template>
    </g-autocomplete>
  `
})
export class CustomTemplateComponent {
  selectedCountry = signal('');

  countries = [
    { label: 'United States', value: 'us', code: 'US', flag: '🇺🇸' },
    { label: 'United Kingdom', value: 'uk', code: 'GB', flag: '🇬🇧' },
    { label: 'Vietnam', value: 'vn', code: 'VN', flag: '🇻🇳' },
    { label: 'Japan', value: 'jp', code: 'JP', flag: '🇯🇵' },
    { label: 'South Korea', value: 'kr', code: 'KR', flag: '🇰🇷' }
  ];
}
```

### With Grouping | Với Nhóm

```typescript
@Component({
  template: `
    <g-autocomplete
      [options]="groupedOptions"
      [(value)]="selectedValue"
      [grouped]="true"
      placeholder="Search... | Tìm kiếm...">
    </g-autocomplete>
  `
})
export class GroupedAutocompleteComponent {
  selectedValue = signal('');

  groupedOptions = [
    {
      group: 'Fruits | Trái cây',
      items: [
        { label: 'Apple', value: 'apple' },
        { label: 'Banana', value: 'banana' }
      ]
    },
    {
      group: 'Vegetables | Rau củ',
      items: [
        { label: 'Carrot', value: 'carrot' },
        { label: 'Tomato', value: 'tomato' }
      ]
    }
  ];
}
```

### Disabled State | Trạng Thái Vô Hiệu Hóa

```html
<g-autocomplete
  [options]="options"
  [(value)]="value"
  [disabled]="true"
  placeholder="Disabled">
</g-autocomplete>
```

### Reactive Forms | Forms Phản Ứng

```typescript
import { FormBuilder, FormGroup, ReactiveFormsModule } from '@angular/forms';
import { Validators } from '@angular/forms';

@Component({
  imports: [AutocompleteComponent, ReactiveFormsModule],
  template: `
    <form [formGroup]="form" (ngSubmit)="submitForm()">
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-2">
            City | Thành Phố
          </label>
          <g-autocomplete
            formControlName="city"
            [options]="cities"
            placeholder="Select city... | Chọn thành phố...">
          </g-autocomplete>
          @if (form.get('city')?.hasError('required') && form.get('city')?.touched) {
            <p class="text-sm text-red-600 mt-1">
              City is required | Thành phố là bắt buộc
            </p>
          }
        </div>

        <button
          type="submit"
          class="px-4 py-2 bg-primary-600 text-white rounded-lg hover:bg-primary-700"
          [disabled]="form.invalid">
          Submit | Gửi
        </button>
      </div>
    </form>
  `
})
export class CityFormComponent {
  form: FormGroup;

  cities = [
    { label: 'New York', value: 'ny' },
    { label: 'Los Angeles', value: 'la' },
    { label: 'Chicago', value: 'chi' },
    { label: 'Houston', value: 'hou' }
  ];

  constructor(private fb: FormBuilder) {
    this.form = this.fb.group({
      city: ['', Validators.required]
    });
  }

  submitForm() {
    if (this.form.valid) {
      console.log('Form submitted:', this.form.value);
    }
  }
}
```

## Props | Thuộc Tính

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `options` | `AutocompleteOption[]` | `[]` | Array of autocomplete options <br/> Mảng các tùy chọn tự động hoàn thành |
| `value` | `any` | - | Selected value <br/> Giá trị được chọn |
| `placeholder` | `string` | `''` | Input placeholder <br/> Placeholder của ô nhập |
| `loading` | `boolean` | `false` | Show loading indicator <br/> Hiển thị chỉ báo đang tải |
| `disabled` | `boolean` | `false` | Disable autocomplete <br/> Vô hiệu hóa tự động hoàn thành |
| `grouped` | `boolean` | `false` | Enable grouped options <br/> Bật tùy chọn nhóm |
| `minLength` | `number` | `1` | Minimum characters to trigger search <br/> Số ký tự tối thiểu để kích hoạt tìm kiếm |
| `debounceTime` | `number` | `300` | Debounce time in ms <br/> Thời gian debounce (ms) |

### AutocompleteOption Interface

```typescript
interface AutocompleteOption {
  label: string;      // Display label | Nhãn hiển thị
  value: any;         // Option value | Giá trị tùy chọn
  disabled?: boolean; // Disable this option | Vô hiệu hóa tùy chọn này
  [key: string]: any; // Additional properties | Thuộc tính bổ sung
}
```

### Outputs | Sự Kiện

| Event | Type | Description |
|-------|------|-------------|
| `valueChange` | `any` | Emitted when selection changes <br/> Phát ra khi lựa chọn thay đổi |
| `search` | `string` | Emitted when user types <br/> Phát ra khi người dùng nhập |

## Accessibility | Khả Năng Tiếp Cận

- Uses semantic input element with role="combobox"
- Proper ARIA attributes (aria-autocomplete, aria-expanded, aria-activedescendant)
- Keyboard navigation (Arrow keys, Enter, Escape)
- Focus management
- Screen reader announcements for results

## Examples | Ví Dụ

### Search with API | Tìm Kiếm với API

```typescript
@Component({
  template: `
    <div class="space-y-4">
      <g-autocomplete
        [options]="searchResults()"
        [(value)]="selectedProduct"
        [loading]="isLoading()"
        (search)="searchProducts($event)"
        placeholder="Search products... | Tìm sản phẩm...">
        <ng-template #optionTemplate let-option>
          <div class="flex items-center gap-3">
            <img
              [src]="option.image"
              [alt]="option.label"
              class="w-10 h-10 rounded object-cover">
            <div class="flex-1">
              <div class="font-medium">{{ option.label }}</div>
              <div class="text-sm text-gray-500">
                ${{ option.price }}
              </div>
            </div>
          </div>
        </ng-template>
      </g-autocomplete>

      @if (selectedProduct()) {
        <div class="p-4 border rounded-lg dark:border-gray-700">
          <h4 class="font-medium mb-2">Selected Product | Sản Phẩm Đã Chọn</h4>
          <pre>{{ getSelectedProductDetails() | json }}</pre>
        </div>
      }
    </div>
  `
})
export class ProductSearchComponent {
  selectedProduct = signal('');
  searchResults = signal<any[]>([]);
  isLoading = signal(false);

  async searchProducts(query: string) {
    if (!query || query.length < 2) {
      this.searchResults.set([]);
      return;
    }

    this.isLoading.set(true);

    try {
      // Simulate API call | Mô phỏng gọi API
      const results = await this.apiSearch(query);
      this.searchResults.set(results);
    } finally {
      this.isLoading.set(false);
    }
  }

  async apiSearch(query: string): Promise<any[]> {
    return new Promise(resolve => {
      setTimeout(() => {
        const products = [
          {
            label: 'Laptop Pro',
            value: 'laptop-pro',
            price: 1299,
            image: '/laptop.jpg'
          },
          {
            label: 'Wireless Mouse',
            value: 'mouse',
            price: 29,
            image: '/mouse.jpg'
          },
          {
            label: 'Mechanical Keyboard',
            value: 'keyboard',
            price: 89,
            image: '/keyboard.jpg'
          }
        ];
        resolve(products.filter(p =>
          p.label.toLowerCase().includes(query.toLowerCase())
        ));
      }, 300);
    });
  }

  getSelectedProductDetails() {
    const product = this.searchResults().find(
      p => p.value === this.selectedProduct()
    );
    return product || {};
  }
}
```

### Tag Input | Nhập Tag

```typescript
@Component({
  template: `
    <div class="space-y-4">
      <div>
        <label class="block text-sm font-medium mb-2">
          Add Tags | Thêm Thẻ
        </label>
        <g-autocomplete
          [options]="availableTags"
          [(value)]="currentTag"
          (valueChange)="addTag($event)"
          placeholder="Type to add tags... | Nhập để thêm thẻ...">
        </g-autocomplete>
      </div>

      <div class="flex flex-wrap gap-2">
        @for (tag of selectedTags(); track tag) {
          <span class="inline-flex items-center gap-2 px-3 py-1 bg-primary-100 dark:bg-primary-900 text-primary-800 dark:text-primary-200 rounded-full text-sm">
            {{ tag }}
            <button
              (click)="removeTag(tag)"
              class="hover:text-primary-600 dark:hover:text-primary-400">
              ✕
            </button>
          </span>
        }
      </div>
    </div>
  `
})
export class TagInputComponent {
  currentTag = signal('');
  selectedTags = signal<string[]>([]);

  availableTags = [
    { label: 'JavaScript', value: 'javascript' },
    { label: 'TypeScript', value: 'typescript' },
    { label: 'Angular', value: 'angular' },
    { label: 'React', value: 'react' },
    { label: 'Vue', value: 'vue' },
    { label: 'Node.js', value: 'nodejs' }
  ];

  addTag(tag: string) {
    if (tag && !this.selectedTags().includes(tag)) {
      this.selectedTags.update(tags => [...tags, tag]);
      this.currentTag.set('');
    }
  }

  removeTag(tag: string) {
    this.selectedTags.update(tags => tags.filter(t => t !== tag));
  }
}
```

### Location Search | Tìm Kiếm Địa Điểm

```typescript
@Component({
  template: `
    <div class="space-y-4">
      <div>
        <label class="block text-sm font-medium mb-2">
          Search Location | Tìm Địa Điểm
        </label>
        <g-autocomplete
          [options]="locations()"
          [(value)]="selectedLocation"
          [loading]="isSearching()"
          (search)="searchLocations($event)"
          placeholder="Enter address... | Nhập địa chỉ...">
          <ng-template #optionTemplate let-option>
            <div class="flex items-start gap-2">
              <span class="text-lg">📍</span>
              <div>
                <div class="font-medium">{{ option.label }}</div>
                <div class="text-xs text-gray-500">
                  {{ option.address }}
                </div>
              </div>
            </div>
          </ng-template>
        </g-autocomplete>
      </div>

      @if (selectedLocation()) {
        <div class="p-4 bg-blue-50 dark:bg-blue-900/20 rounded-lg">
          <h4 class="font-medium text-blue-900 dark:text-blue-100 mb-2">
            Selected Location | Địa Điểm Đã Chọn
          </h4>
          <p class="text-sm text-blue-800 dark:text-blue-200">
            {{ getLocationDetails() }}
          </p>
        </div>
      }
    </div>
  `
})
export class LocationSearchComponent {
  selectedLocation = signal('');
  locations = signal<any[]>([]);
  isSearching = signal(false);

  async searchLocations(query: string) {
    if (!query || query.length < 3) {
      this.locations.set([]);
      return;
    }

    this.isSearching.set(true);

    try {
      // Simulate geocoding API | Mô phỏng API geocoding
      const results = await this.geocode(query);
      this.locations.set(results);
    } finally {
      this.isSearching.set(false);
    }
  }

  async geocode(query: string): Promise<any[]> {
    return new Promise(resolve => {
      setTimeout(() => {
        const places = [
          {
            label: 'Hanoi, Vietnam',
            value: 'hanoi',
            address: 'Capital of Vietnam',
            lat: 21.0285,
            lng: 105.8542
          },
          {
            label: 'Ho Chi Minh City, Vietnam',
            value: 'hcm',
            address: 'Largest city in Vietnam',
            lat: 10.8231,
            lng: 106.6297
          }
        ];
        resolve(places.filter(p =>
          p.label.toLowerCase().includes(query.toLowerCase())
        ));
      }, 500);
    });
  }

  getLocationDetails(): string {
    const location = this.locations().find(
      l => l.value === this.selectedLocation()
    );
    return location ? `${location.label} - ${location.address}` : '';
  }
}
```

## Best Practices | Thực Hành Tốt Nhất

1. **Debounce search** | **Debounce tìm kiếm**: Use debouncing to avoid excessive API calls

2. **Loading states** | **Trạng thái đang tải**: Show loading indicator during async operations

3. **Empty states** | **Trạng thái rỗng**: Provide helpful message when no results

4. **Minimum characters** | **Ký tự tối thiểu**: Set minimum length before triggering search

5. **Clear option** | **Tùy chọn xóa**: Provide way to clear selection

6. **Keyboard support** | **Hỗ trợ bàn phím**: Ensure full keyboard navigation

## Related Components | Components Liên Quan

- [Select](../../select) - Dropdown selection | Chọn thả xuống
- [Input](../../input) - Text input | Nhập văn bản
- [Combobox](../combobox) - Combo selection | Chọn combo
